{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/commands/install.result.schema.json",
  "$ref": "#/$defs/InstallResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "InstallAction": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path is the repository-relative file path touched by the action."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the coarse action kind such as create, update, or noop."
        },
        "details": {
          "type": "string",
          "description": "Details is the human-readable explanation of the planned change."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "kind",
        "details"
      ],
      "description": "InstallAction describes one file-level change planned or applied by `harness install`."
    },
    "InstallResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether the command succeeded."
        },
        "command": {
          "type": "string",
          "description": "Command is the stable command identifier for the result payload."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable outcome description."
        },
        "mode": {
          "type": "string",
          "description": "Mode indicates whether the command applied changes or only planned them."
        },
        "scope": {
          "type": "string",
          "description": "Scope is the resolved install scope."
        },
        "actions": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/InstallAction"
              },
              "type": "array",
              "description": "Actions lists the planned or executed file-level install actions."
            },
            {
              "type": "null"
            }
          ],
          "description": "Actions lists the planned or executed file-level install actions."
        },
        "next_actions": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/NextAction"
              },
              "type": "array",
              "description": "NextAction lists the most relevant follow-up steps in priority order."
            },
            {
              "type": "null"
            }
          ],
          "description": "NextAction lists the most relevant follow-up steps in priority order."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented install preparation or writes."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "command",
        "summary",
        "mode",
        "scope",
        "actions",
        "next_actions"
      ],
      "description": "InstallResult is the JSON result returned by `harness install`."
    },
    "NextAction": {
      "properties": {
        "command": {
          "oneOf": [
            {
              "type": "string",
              "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
            },
            {
              "type": "null"
            }
          ],
          "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
        },
        "description": {
          "type": "string",
          "description": "Description explains the suggested next step in plain language."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "command",
        "description"
      ],
      "description": "NextAction describes one concrete follow-up action that the caller should consider after reading a command result."
    }
  },
  "title": "Install command result",
  "description": "JSON output for `harness install`."
}
